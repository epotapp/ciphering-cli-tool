const { expect }= require('@jest/globals');
const TransRot8Dec = require('../src/transrot8dec');

test("Sanity check", () => {
    expect(true).toBe(true);
});

describe('rot8DecFunc', () =>  {
    it('')
    test("Decoding Rot8", () => {
        
        expect()
    });
});